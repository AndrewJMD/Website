machine:
  java:
    version: oraclejdk8
dependencies:
  pre:
    - sudo pip install html5validator
    - sudo pip install Pillow
test:
  override:
    - html5validator --root .
    - python ./build/tests/filenames.py .
    - python ./build/tests/existence.py .
    - sh ./build/tests/line-endings.sh
